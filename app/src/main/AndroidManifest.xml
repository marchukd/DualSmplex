<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dmytro.dualsmplex" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyTheme" >
        <activity
            android:name=".EnterVarLimCount"
            android:label="@string/app_name" />
        <activity
            android:name=".SelectMethodActivity"
            android:label="@string/title_activity_select_method" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GenerationGridActivity"
            android:label="@string/title_activity_generation_grid" >
        </activity>
    </application>

</manifest>
